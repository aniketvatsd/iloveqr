# Push to GitHub

Remote is set. From **Git Bash** or **PowerShell** in this folder run:

```bash
git branch -M main
git push -u origin main
```

**Remote already set:** `origin` → https://github.com/aniketvatsdwivedi/ilovveqr.git

Ensure the repo **ilovveqr** exists on GitHub (create at https://github.com/new if needed), then run the two commands above.
